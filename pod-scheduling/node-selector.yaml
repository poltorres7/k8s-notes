apiVersion: v1
kind: Pod
metadata:
  name: nginx-<NAME>
  labels:
    app: nginx
spec:
  containers:
  - name: nginx
    image: nginx
    imagePullPolicy: IfNotPresent
  nodeSelector:
    curso: CEROUNO
